# Unit Tests

Unit tests cover validation and helper behavior used by the Worker and frontend workflow.

Current files:

- `validation.test.ts`
- `helpers.test.ts`

Run with:

```bash
npm run test:run
```
